name: toxic-nlp
channels:
  - defaults
dependencies:
  - python=3.12
  - ipykernel
  - numpy
  - scikit-learn
  - pandas
  - matplotlib
  - jupyter
  - pyarrow
  - nltk
  - tqdm
  - pytest
  - pip
  - pip:
      - fasttext-wheel
      - contractions
      - regex
prefix: /opt/miniconda3/envs/toxic-nlp
